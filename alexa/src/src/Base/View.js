//Importamos todas las clases que necesita la Vista (Arquitectura VIP).
const APLBuilder = require('../Views/APLBuilder');
const ViewManager = require('../Views/ViewManager');
const ResponseBuilder = require('../Views/ResponseBuilder');

//Exportamos todas las clases que necesita la Vista (Arquitectura VIP).
module.exports.APLBuilder = APLBuilder;
module.exports.ViewManager = ViewManager;
module.exports.ResponseBuilder = ResponseBuilder;